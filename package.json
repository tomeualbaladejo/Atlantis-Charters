{
  "name": "atlantis-charters",
  "version": "1.0.0",
  "scripts": {
    "build": "echo 'Using pre-built dist'",
    "start": "npx serve dist -p $PORT --single"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
